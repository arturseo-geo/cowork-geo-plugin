---
description: Score any URL or content against the 5-layer GEO Stack. Returns per-layer scores (0–10), combined GEO score (0–100), and a prioritised fix list.
---

Use the geo-scoring skill to evaluate AI search visibility.

Target: $ARGUMENTS

If a URL is provided, fetch the page and score it.
If content is pasted directly, score it from the paste.
If no argument is given, ask the user for a URL or content to score.

Return the full GEO score report with deviation map and priority fix list.
