---
description: Audit JSON-LD structured data on any URL and output corrected, GEO-optimised schema ready to deploy.
---

Use the schema-optimization skill to audit and fix structured data.

Target: $ARGUMENTS

If a URL is provided, fetch the page and audit all JSON-LD blocks.
If JSON-LD is pasted directly, audit it from the paste.
If no argument, ask the user for a URL or schema to check.

Return the full audit report and complete corrected JSON-LD blocks.
